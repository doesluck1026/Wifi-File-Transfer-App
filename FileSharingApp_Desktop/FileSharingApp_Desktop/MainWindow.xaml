<Window x:Class="FileSharingApp_Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileSharingApp_Desktop"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height = "1*" />
            <RowDefinition Height = "3*" />
            <RowDefinition Height = "3*" />
            <RowDefinition Height = "3*" />
        </Grid.RowDefinitions>

        <!-- Information bar  -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="AliceBlue">
            <Label Content=" ==> : " VerticalAlignment="Center" />
            <TextBlock x:Name="txt_StatusInfo" VerticalAlignment="Center" Text="" />
        </StackPanel>

        <!-- Step 1, choice of transaction type  -->
        <Border Grid.Row="1" Padding="10" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width = "1*" />
                    <ColumnDefinition Width = "3*" />
                    <ColumnDefinition Width = "3*" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Border Grid.Column = "0" Padding = "10" Grid.RowSpan="2">
                    <Label x:Name="lbl_FirstStep" Background="Green" />

                </Border>

                <Border Grid.Column = "1" Padding = "10">
                    <Button x:Name="btn_SendFile" Content=" Send File" Click="btn_SendFile_Click" >
                        
                    </Button>
                    
                </Border>

                <Border Grid.Column = "2" Padding = "10">
                    <Button x:Name="btn_ReceiveFile" Content=" Receive File" Click="btn_ReceiveFile_Click" >

                    </Button>

                </Border>

                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Grid.ColumnSpan="2" >
                    <Label Content=" File Path  : " VerticalAlignment="Center" />
                    <TextBlock x:Name="txt_FilePath" VerticalAlignment="Center" Text="" />

                </StackPanel>

            </Grid>
        </Border>

        <!-- Step 2, Confirmation of the file to be sent or received  -->
        <Border x:Name="bdr_Step2" Grid.Row="2" Padding="10" IsEnabled="True" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width = "1*" />
                    <ColumnDefinition Width = "3*" />
                    <ColumnDefinition Width = "3*" />
                </Grid.ColumnDefinitions>

                <Border  Grid.Column = "0" Padding = "10">
                    <Label x:Name="lbl_SecondStep" Background="LightSkyBlue" />

                </Border>

                <Border  Grid.Column = "1" Padding = "10">
                    <Grid >
                        <Grid.RowDefinitions>
                            <RowDefinition Height = "*" />
                            <RowDefinition Height = "*" />
                            <RowDefinition Height = "*" />
                        </Grid.RowDefinitions>

                        <Label Grid.Row="0" Content=" File Info      : " VerticalAlignment="Center" Background="#eee"/>
                        <StackPanel Grid.Row="1" Orientation="Horizontal">
                            <Label Content=" File Name  : " VerticalAlignment="Center" />
                            <TextBlock x:Name="txt_FileName" VerticalAlignment="Center" Text="" />

                        </StackPanel>

                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <Label Content=" File Size      : " VerticalAlignment="Center" />
                            <TextBlock x:Name="txt_FileSize" VerticalAlignment="Center" Text="" />

                        </StackPanel>
                        
                    </Grid>
                    
                </Border>
                
                <Border  Grid.Column = "3" Padding = "10">
                    <Grid >
                        <Grid.RowDefinitions>
                            <RowDefinition Height = "*" />
                            <RowDefinition Height = "*" />
                            <RowDefinition Height = "*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Padding="2" HorizontalAlignment="Center">

                            <TextBox x:Name="txt_IpCode" Padding="-10" VerticalAlignment="Center" HorizontalAlignment="Center" Text=" CODE" FontSize="35" FontWeight="Bold" />

                        </Border>

                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" >
                            <Label Content="Host Name  : " VerticalAlignment="Center" />
                            <TextBlock x:Name="txt_HostName" VerticalAlignment="Center" Text="" />

                        </StackPanel>

                        <Border Grid.Row="2" Padding="1">
                            <Grid>
                                <Button x:Name="btn_Confirm" Content="Confirm" FontWeight="Bold" Click="btn_Confirm_Click"/>

                            </Grid>

                        </Border>

                    </Grid>

                </Border>
            </Grid>
        </Border>

        <!-- Step 3, tracking the sending or receiving process -->
        <Border x:Name="bdr_Step3" Grid.Row="3" Padding="10" IsEnabled="False" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width = "1*" />
                    <ColumnDefinition Width = "6*" />

                </Grid.ColumnDefinitions>

                <Border  Grid.Column = "0" Padding = "10">
                    <Label x:Name="lbl_ThirdStep" Background="LightBlue" />

                </Border>

                <Grid Grid.Column = "1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height = "*" />
                        <RowDefinition Height = "*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row = "0" Margin="15">
                        <ProgressBar Minimum="0" Maximum="100" Value="0" Name="pbStatus" />
                        <TextBlock Text="{Binding ElementName=pbStatus, Path=Value, StringFormat={}{0:0}%}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Grid>

                    <Grid Grid.Row = "1" Margin="10">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <Label Content=" Speed : " VerticalAlignment="Center"/>
                                <TextBlock x:Name="txt_TransferSpeed" VerticalAlignment="Center" Text="" />

                            </StackPanel>
                            
                        </Grid>    

                    </Grid>

                </Grid>
                
            </Grid>
            
        </Border>
    </Grid>
</Window>
